# Настройка отправки откликов на вакансии по email

## ✅ Что уже настроено

Код уже настроен для отправки откликов на **два адреса**:
1. **Email из вакансии** (если указан в поле `email` вакансии)
2. **79644228177@mail.ru** (всегда, даже если email вакансии не указан)

## 📋 Что нужно сделать

### Шаг 1: Получить пароль приложения из Яндекс 360

⚠️ **ВАЖНО**: Для SMTP нужен **пароль приложения**, а не основной пароль от почты!

1. Откройте https://id.yandex.ru/security
2. Войдите в аккаунт Яндекс 360 (где у вас настроен домен uchiteltut.ru)
3. Найдите раздел **"Пароли приложений"**
   - Если не видите, включите двухфакторную аутентификацию (2FA)
4. Нажмите **"Создать новый пароль"** или **"Создать пароль приложения"**
5. Выберите тип: **"Почта"** или **"Другое"**
   - Если "Другое", введите: `SMTP для uchiteltut.ru`
6. Нажмите **"Создать"**
7. **Скопируйте пароль** (он показывается только один раз!)
   - Пароль выглядит как строка букв и цифр: `abcdefghijklmnop`

### Шаг 2: Определить email для отправки

Вам нужен email ящик в Яндекс 360 для отправки писем. Например:
- `noreply@uchiteltut.ru`
- `no-reply@uchiteltut.ru`
- Или любой другой ящик в вашем домене

### Шаг 3: Настроить переменные окружения

#### Если вы работаете локально:

1. Создайте файл `.env` в папке `newServer` (если его нет)
2. Скопируйте настройки из `env.example`:
   ```bash
   cd newServer
   cp ../env.example .env
   ```

3. Откройте файл `.env` и добавьте/обновите следующие строки:
   ```env
   SMTP_HOST=smtp.yandex.ru
   SMTP_PORT=465
   SMTP_SECURE=true
   SMTP_USER=ваш-email@uchiteltut.ru
   SMTP_PASS=ваш_пароль_приложения_из_шага_1
   MAIL_FROM="УчительТут <ваш-email@uchiteltut.ru>"
   ```

4. Сохраните файл

#### Если вы работаете на сервере (VPS):

1. Подключитесь к серверу:
   ```bash
   ssh root@91.229.9.105
   ```

2. Перейдите в директорию проекта:
   ```bash
   cd /opt/uchiteltut
   ```

3. Откройте файл `.env`:
   ```bash
   nano .env
   ```

4. Добавьте/обновите следующие строки:
   ```env
   SMTP_HOST=smtp.yandex.ru
   SMTP_PORT=465
   SMTP_SECURE=true
   SMTP_USER=ваш-email@uchiteltut.ru
   SMTP_PASS=ваш_пароль_приложения_из_шага_1
   MAIL_FROM="УчительТут <ваш-email@uchiteltut.ru>"
   ```

5. Сохраните файл:
   - Нажмите `Ctrl + O` (сохранить)
   - Нажмите `Enter` (подтвердить)
   - Нажмите `Ctrl + X` (выйти)

6. Перезапустите сервер:
   ```bash
   docker-compose -f docker-compose.prod.yml restart server
   ```

   Или если нужно пересобрать:
   ```bash
   docker-compose -f docker-compose.prod.yml up -d --build server
   ```

### Шаг 4: Проверить работу

1. Откройте сайт с вакансиями
2. Найдите любую вакансию
3. Нажмите **"Откликнуться на вакансию"**
4. Заполните форму (ФИО, телефон, образование, опыт работы)
5. При необходимости прикрепите резюме
6. Нажмите **"Отправить отклик"**

7. Проверьте почту:
   - На email из вакансии (если он указан)
   - На **79644228177@mail.ru** (всегда должен прийти)

### Шаг 5: Проверить логи (если что-то не работает)

#### На сервере:
```bash
docker-compose -f docker-compose.prod.yml logs -f server
```

Ищите сообщения:
- ✅ `SMTP connection verified` - подключение успешно
- ✅ `Email sent successfully` - письмо отправлено
- ❌ Любые ошибки подключения - проверьте настройки

#### Локально:
Логи будут выводиться в консоль, где запущен сервер.

## 🔍 Как это работает

1. Пользователь нажимает **"Откликнуться на вакансию"** на странице вакансии
2. Заполняет форму (ФИО, телефон, образование, опыт работы, резюме)
3. При отправке формы данные отправляются на `/api/respond`
4. Сервер:
   - Проверяет наличие SMTP настроек
   - Подключается к SMTP серверу Яндекс 360
   - Формирует письмо с данными кандидата
   - Отправляет письмо на:
     - Email из вакансии (если указан)
     - **79644228177@mail.ru** (всегда)
   - Прикрепляет резюме, если оно было загружено

## ⚠️ Важные моменты

1. **Пароль приложения** - это НЕ ваш основной пароль от почты. Его нужно создать отдельно в настройках безопасности Яндекс ID.

2. **Пароль показывается только один раз** - сохраните его в безопасном месте.

3. **Если email вакансии не указан** - письмо все равно отправится на 79644228177@mail.ru.

4. **Дубликаты автоматически удаляются** - если email вакансии совпадает с 79644228177@mail.ru, письмо отправится только один раз.

5. **Формат письма**:
   - HTML версия - красиво оформленное письмо
   - Текстовая версия - для почтовых клиентов без поддержки HTML
   - Резюме прикрепляется как вложение

## 🐛 Решение проблем

### Ошибка: "SMTP не настроен"
- Проверьте, что в `.env` файле указаны `SMTP_USER` и `SMTP_PASS`
- Убедитесь, что файл `.env` находится в правильной директории
- Перезапустите сервер после изменения `.env`

### Ошибка: "Ошибка подключения к SMTP серверу"
- Проверьте правильность пароля приложения
- Убедитесь, что используете пароль приложения, а не основной пароль
- Проверьте, что домен верифицирован в Яндекс 360
- Проверьте настройки файрвола (порт 465 должен быть открыт)

### Письма не приходят
- Проверьте папку "Спам" на обоих адресах
- Проверьте логи сервера на наличие ошибок
- Убедитесь, что email адреса указаны правильно
- Попробуйте отправить тестовое письмо через другой способ

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи сервера
2. Убедитесь, что все настройки указаны правильно
3. Проверьте документацию Яндекс 360: https://yandex.ru/support/business/mail/mail-clients.html

