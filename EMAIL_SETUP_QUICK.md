# Быстрая настройка отправки писем через Яндекс 360

## Что было сделано

✅ Настроена отправка писем при отклике на вакансию
✅ Письма отправляются на email из вакансии
✅ Автоматически дублируются на `79644228177@mail.ru`
✅ Добавлена HTML версия писем
✅ Улучшена обработка ошибок

## Что нужно сделать

### 1. Создать почтовый ящик в Яндекс 360

1. Войдите в панель Яндекс 360
2. Создайте почтовый ящик (например: `noreply@uchiteltut.ru`)

### 2. Создать пароль приложения

⚠️ **ВАЖНО**: Нужен именно **пароль приложения**, а не основной пароль!

**Быстрый способ:**
1. Откройте: https://id.yandex.ru/security/app-passwords
2. Войдите в аккаунт с ящиком `no-reply@uchiteltut.ru`
3. Нажмите **"Создать новый пароль"**
4. Выберите **"Почта"** или **"Другое"** → укажите `SMTP для uchiteltut.ru`
5. **Скопируйте пароль** (он показывается только один раз!)

**Если не видите "Пароли приложений":**
- Включите двухфакторную аутентификацию (2FA) в настройках безопасности
- После этого появится раздел "Пароли приложений"

📖 **Подробная инструкция:** [YANDEX360_PASSWORD_GUIDE.md](./YANDEX360_PASSWORD_GUIDE.md)

### 3. Настроить на сервере

#### Для production (VPS):

```bash
ssh root@91.229.9.105
cd /opt/uchiteltut
nano .env
```

Добавьте/обновите:

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=noreply@uchiteltut.ru
SMTP_PASS=ваш_пароль_приложения_из_шага_2
MAIL_FROM="УчительТут <noreply@uchiteltut.ru>"
```

Сохраните (Ctrl+O, Enter, Ctrl+X) и перезапустите:

```bash
docker-compose -f docker-compose.prod.yml restart server
```

#### Для локальной разработки:

Создайте файл `.env` в корне проекта или в папке `newServer`:

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=ваш_email@uchiteltut.ru
SMTP_PASS=ваш_пароль_приложения
MAIL_FROM="УчительТут <noreply@uchiteltut.ru>"
```

## Проверка

1. Откройте сайт
2. Найдите любую вакансию с указанным email
3. Нажмите "Откликнуться на вакансию"
4. Заполните форму и отправьте
5. Проверьте почту:
   - Email из вакансии должен получить письмо
   - `79644228177@mail.ru` должен получить копию

## Устранение проблем

### Письма не отправляются

1. Проверьте логи сервера:
   ```bash
   docker-compose -f docker-compose.prod.yml logs server
   ```

2. Убедитесь, что:
   - ✅ Используется пароль приложения (не основной пароль)
   - ✅ Email ящика правильный
   - ✅ Домен верифицирован в Яндекс 360
   - ✅ Переменные окружения установлены правильно

3. Проверьте подключение к SMTP:
   - В логах должно быть: `SMTP connection verified`
   - Если ошибка - проверьте настройки

### Ошибка "SMTP не настроен"

Убедитесь, что в `.env` файле указаны:
- `SMTP_USER`
- `SMTP_PASS`

### Ошибка подключения к SMTP

- Проверьте, что пароль приложения создан правильно
- Убедитесь, что порт 465 не заблокирован
- Проверьте, что домен верифицирован в Яндекс 360

## Дополнительная информация

Подробная документация: [YANDEX360_SETUP.md](./YANDEX360_SETUP.md)

