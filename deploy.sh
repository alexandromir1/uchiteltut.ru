#!/bin/bash

# Скрипт для автоматического деплоя на VPS

set -e

echo "🚀 Начинаем деплой..."

# Проверяем, что мы в правильной директории
if [ ! -f "docker-compose.prod.yml" ]; then
    echo "❌ Ошибка: docker-compose.prod.yml не найден"
    exit 1
fi

# Проверяем наличие .env файла
if [ ! -f ".env" ]; then
    echo "⚠️  Файл .env не найден. Создайте его из env.example"
    exit 1
fi

# Получаем последние изменения из git (если есть)
if [ -d ".git" ]; then
    echo "📥 Получаем последние изменения из git..."
    git pull || echo "⚠️  Не удалось обновить из git"
fi

# Собираем образы
echo "🔨 Собираем Docker образы..."
docker-compose -f docker-compose.prod.yml build

# Останавливаем старые контейнеры
echo "🛑 Останавливаем старые контейнеры..."
docker-compose -f docker-compose.prod.yml down

# Запускаем новые контейнеры
echo "▶️  Запускаем новые контейнеры..."
docker-compose -f docker-compose.prod.yml up -d

# Ждем, пока база данных будет готова
echo "⏳ Ожидаем готовности базы данных..."
sleep 10

# Применяем миграции
echo "📊 Применяем миграции базы данных..."
docker-compose -f docker-compose.prod.yml exec -T server npx prisma migrate deploy || echo "⚠️  Миграции уже применены или ошибка"

# Проверяем статус
echo "✅ Проверяем статус сервисов..."
docker-compose -f docker-compose.prod.yml ps

echo ""
echo "🎉 Деплой завершен!"
echo ""
echo "Проверьте логи: docker-compose -f docker-compose.prod.yml logs -f"
echo "Проверьте статус: docker-compose -f docker-compose.prod.yml ps"

